name: CI/CD Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Install system dependencies for pyodbc
        run: |
          sudo apt-get update
          sudo apt-get install -y unixodbc-dev

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.8'

      - name: Install dependencies
        run: |
          cd .github/azure-three-tier-app/src/backend
          pip install -r requirements.txt
          pip install pytest

      - name: Run backend tests
        run: |
          cd .github/azure-three-tier-app/src/backend
          pytest

      - name: Build frontend
        run: |
          cd .github/azure-three-tier-app/src/frontend
          npm install
          npm run build

  deploy:
    runs-on: ubuntu-latest
    needs: build

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Log in to Azure
      uses: azure/login@v2
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}

    - name: Terraform Init
      run: |
        cd .github/azure-three-tier-app/infrastructure
        terraform init

    - name: Terraform Plan
      run: |
        cd .github/azure-three-tier-app/infrastructure
        terraform plan

    - name: Terraform Apply
      run: |
        cd .github/azure-three-tier-app/infrastructure
        terraform apply -auto-approve

    - name: Deploy Logic App
      run: |
        az logic app deploy --resource-group <your-resource-group> --name <your-logic-app-name> --source <path-to-your-logicapp.json>